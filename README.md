# nextjs-boboilerplate

# コンテナ起動

```sh
$ docker-compose up -d
```

# コンテナ停止

```sh
$ docker-compose down
```

# 初回実行

```sh
$ docker-compose exec web yarn install
```

# サーバー起動

```sh
$ docker-compose exec web yarn dev
```
